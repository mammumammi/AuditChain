## Packages
recharts | Complex data visualizations for the audit metrics
framer-motion | Premium staggered animations and micro-interactions
date-fns | Formatting timestamps for blockchain events
lucide-react | UI icons
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
  mono: ["var(--font-mono)"],
}
